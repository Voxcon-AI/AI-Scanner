# Story 1.1: Project Initialization and Monorepo Setup

## Status

Ready for Review

## Story

**As a** developer,
**I want** a properly initialized Node.js monorepo with shared workspace configuration,
**so that** all services can share common utilities and dependencies efficiently.

## Acceptance Criteria

1. Git repository initialized with .gitignore excluding node_modules, .env, and common Node.js artifacts
2. Root package.json configured with workspaces for services/ subdirectories (watcher, worker, api, mailer)
3. Node.js v24.5.0 specified in package.json engines field and .nvmrc file
4. Shared workspace created at shared/ directory with common utilities (logger config, constants, types)
5. ESLint and Prettier configured for consistent code formatting across all services
6. README.md includes project overview and links to setup documentation
7. LICENSE file added (MIT or Apache 2.0 for open source)
8. .env.example file created with placeholder values for all required environment variables

## Tasks / Subtasks

- [x] Initialize Git repository and configure .gitignore (AC: 1)
  - [x] Run `git init` if not already initialized
  - [x] Create .gitignore excluding: node_modules/, .env, .env.local, dist/, coverage/, .DS_Store, \*.log
  - [x] Verify .gitignore prevents sensitive files from being tracked

- [x] Create root package.json with workspaces configuration (AC: 2, 3)
  - [x] Initialize root package.json with `npm init -y`
  - [x] Add workspaces array: `["services/*", "shared"]`
  - [x] Set engines.node to "24.5.0" in package.json
  - [x] Create .nvmrc file with content "24.5.0"
  - [x] Verify workspace detection with `npm ls --workspaces`

- [x] Create shared workspace with TypeScript configuration (AC: 4)
  - [x] Create directory: shared/src/
  - [x] Create shared/package.json with name "@ai-scanner/shared", main: "dist/index.js", types: "dist/index.d.ts"
  - [x] Create shared/tsconfig.json extending root config, outDir: "dist", declaration: true
  - [x] Create shared/src/types/ for shared TypeScript interfaces
  - [x] Create shared/src/constants/ for shared enums and error codes
  - [x] Create shared/src/utils/ for shared functions (filename sanitization, etc.)
  - [x] Create shared/src/index.ts exporting all types, constants, and utils
  - [x] Run `npm install` in shared/ to initialize workspace

- [x] Configure ESLint and Prettier for code formatting (AC: 5)
  - [x] Install dev dependencies: eslint, prettier, @typescript-eslint/eslint-plugin, @typescript-eslint/parser
  - [x] Create .eslintrc.json with TypeScript parser and recommended rules
  - [x] Create .prettierrc with: semi: true, singleQuote: true, trailingComma: 'es5', tabWidth: 2
  - [x] Add .eslintignore: node_modules/, dist/, coverage/
  - [x] Add npm scripts to root package.json: "lint": "eslint .", "format": "prettier --write ."
  - [x] Test linting with `npm run lint` and formatting with `npm run format`

- [x] Create documentation files (AC: 6, 7)
  - [x] Create README.md with project title, overview, quick start command, and links to docs/
  - [x] Copy LICENSE file (MIT or Apache 2.0) to root directory
  - [x] Verify README includes link to docs/architecture.md and docs/prd.md

- [x] Create .env.example with required environment variables (AC: 8)
  - [x] Document DATABASE_URL with placeholder: "postgresql://user:pass@localhost:5432/ai_scanner"
  - [x] Document REDIS_URL with placeholder: "redis://localhost:6379"
  - [x] Document GEMINI_API_KEY with placeholder: "your-gemini-api-key-here"
  - [x] Document SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS with placeholders
  - [x] Document JWT_PRIVATE_KEY and JWT_PUBLIC_KEY with placeholders (PEM format)
  - [x] Document SCAN_FOLDER_PATH and OUTPUT_FOLDERS_BASE with placeholders
  - [x] Document WEB_UI_BASE_URL with placeholder: "http://localhost:3000"
  - [x] Add comments explaining each environment variable's purpose

- [x] Verify monorepo initialization (All ACs)
  - [x] Run `npm install` at root to initialize workspaces
  - [x] Verify shared package is linked in node_modules/@ai-scanner/shared
  - [x] Test importing from shared: create test file importing a type and compile with tsc
  - [x] Verify .nvmrc is respected: run `nvm use` (if nvm installed)
  - [x] Verify linting works across all workspaces
  - [x] Commit initial structure with message: "chore: initialize monorepo with workspaces and shared package"

## Dev Notes

### Project Structure Context

[Source: architecture/unified-project-structure.md]

The monorepo follows this structure:

- Root directory contains package.json with workspaces configuration
- services/ subdirectory will contain: watcher, worker, api, mailer (created in future stories)
- shared/ workspace at root level for shared TypeScript types and utilities
- Frontend lives in frontend/ directory (static assets, no workspace package)
- Docker Compose configs in docker/ directory
- Documentation in docs/ directory

### Technology Stack

[Source: architecture/tech-stack.md]

- **Node.js Version:** v24.5.0 (specified in package.json engines field and .nvmrc)
- **Build Tool:** esbuild via tsx for TypeScript transpilation (100x faster than tsc)
- **Backend Language:** TypeScript 5.3+ transpiled to JavaScript
- **Bundler:** None for backend (uses Node.js native ESM), esbuild for frontend
- **No ORM:** Using raw SQL with pg library instead of TypeORM/Prisma
- **Workspace Management:** npm workspaces (native to npm 7+)

### Shared Package Structure

[Source: architecture/unified-project-structure.md#L74-79]

The shared/ workspace must export:

- **Types:** TypeScript interfaces (Document, User, ProcessingQueue, Batch, AnalysisResult)
- **Constants:** Enums, error codes, configuration constants
- **Utils:** Shared functions like filename sanitization, path validation

Services import from shared using: `import { Document } from '@ai-scanner/shared'`

### Critical Coding Standards

[Source: architecture/coding-standards.md#L1-22]

- **Type Sharing:** Always define types in shared/src/types/ and import from @ai-scanner/shared. Never duplicate type definitions.
- **Environment Variables:** Access only through config objects, never use process.env directly in business logic. Validate all required env vars on service startup (fail fast if missing).
- **File Names:** Match primary export name (Button.js exports Button class)
- **Async Functions:** Prefix with async keyword, return Promises
- **Constants:** Use SCREAMING_SNAKE_CASE for environment variables and constants

### Git Configuration

[Source: architecture/unified-project-structure.md#L105]

.gitignore must exclude:

- node_modules/ (dependency cache)
- .env, .env.local (sensitive environment variables)
- dist/ (build output)
- coverage/ (test coverage reports)
- .DS_Store (macOS metadata)
- \*.log (log files)

### Environment Variables Required

[Source: architecture/tech-stack.md, architecture/backend-architecture.md]

The .env.example must document these variables:

- **DATABASE_URL:** PostgreSQL connection string
- **REDIS_URL:** Redis connection string
- **GEMINI_API_KEY:** Google Gemini API key for AI analysis
- **SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS:** Email service configuration
- **JWT_PRIVATE_KEY, JWT_PUBLIC_KEY:** RS256 key pair for JWT signing (PEM format)
- **SCAN_FOLDER_PATH:** Absolute path to network share for scanned files
- **OUTPUT_FOLDERS_BASE:** Base path for destination folders
- **WEB_UI_BASE_URL:** Frontend URL for email links

### Linting and Formatting Configuration

[Source: architecture/coding-standards.md]

- ESLint with TypeScript parser (@typescript-eslint/parser)
- Prettier with: semi: true, singleQuote: true, trailingComma: 'es5', tabWidth: 2
- npm scripts for: "lint" (runs eslint), "format" (runs prettier --write)

### TypeScript Configuration

[Source: architecture/tech-stack.md#L21]

- TypeScript 5.3+ for type-safe Node.js services
- Shares types with frontend via shared package
- Transpiled to JavaScript at runtime via tsx (development) or esbuild (production)
- Root tsconfig.json with strict mode enabled
- Shared package tsconfig.json extends root, outputs to dist/ with declaration files

### Testing

[Source: architecture/testing-strategy.md]

Testing framework: Vitest 1.0+

- Unit tests for utilities and business logic
- Integration tests for API endpoints with Supertest
- Test file naming: {source}.test.ts (e.g., filename.test.ts)

Note: Testing setup is not part of this story's scope (deferred to Epic 6)

## Change Log

| Date       | Version | Description            | Author             |
| ---------- | ------- | ---------------------- | ------------------ |
| 2025-10-03 | 1.0     | Initial story creation | Bob (Scrum Master) |

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-5-20250929

### Debug Log References

None

### Completion Notes List

- Successfully initialized monorepo with npm workspaces
- Created @ai-scanner/shared package with TypeScript types, constants, and utilities
- Configured ESLint 9 with flat config format (eslint.config.js) and Prettier
- Added type: "module" to root and shared package.json for ES modules
- Verified workspace symlink at node_modules/@ai-scanner/shared
- Tested imports working correctly with test-import.ts (then removed)
- All tasks and subtasks completed successfully
- Initial commit created on dev branch

### File List

- .gitignore
- .nvmrc
- .prettierrc
- eslint.config.js
- LICENSE
- README.md
- package.json
- tsconfig.json
- .env.example
- shared/package.json
- shared/tsconfig.json
- shared/src/index.ts
- shared/src/types/index.ts
- shared/src/constants/index.ts
- shared/src/utils/index.ts

## QA Results

[To be filled by QA Agent]
